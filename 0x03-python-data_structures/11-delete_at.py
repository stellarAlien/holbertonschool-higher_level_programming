#!/usr/bin/python3
def multiple_returns(sentence):
    if sentence == "":
    return (0, None)
    else:
        return (len(sentence), sentence[0])
    if (idx >= len(my_list) or idx < 0):
        return my_list
    else:
        del my_list[idx]
    return my_list
