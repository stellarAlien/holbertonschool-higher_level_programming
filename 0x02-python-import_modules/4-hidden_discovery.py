#!/usr/bin/python3
    import re
    import sys
    import hidden_4.pyc
    names = dir(hiiden_4)
    for n in names
    for i in (97, 122):
        if((re.match("[^__]{2}[\w]{1,}", n) and n[0] == chr(i)):
                print("{:s}".format(n))
        
